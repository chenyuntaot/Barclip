# AGENTS.md

## 1. 项目定位

本文件用于约束 AI Agent 在本项目中的开发行为、技术选型、代码规范、测试要求、文档维护方式及安全边界。

**重要原则：**

* AGENTS.md 只描述项目级开发规则，不记录具体业务需求、页面设计、接口字段或具体功能实现。
* 具体业务需求应维护在项目开发文档中。
* Agent 在修改代码前，应先理解现有项目结构和开发文档，避免重复实现或破坏既有设计。
* 优先复用已有代码、组件、工具和设计规范，避免无必要地引入新的依赖或架构。

---

## 2. 技术栈

项目采用 **MacOS 原生技术栈**进行开发，目标平台：

* MacOS

核心技术：

* **Swift**：项目主要开发语言
* **SwiftUI**：MacOS 用户界面开发框架

### 平台要求

* 项目**仅面向 MacOS 平台**
* 使用 **Swift + SwiftUI** 进行原生 MacOS 开发
* 不考虑 Android、React Native、Flutter 等跨平台兼容性
* UI 与交互优先遵循 Apple Human Interface Guidelines
* 优先使用 Apple 官方 Framework 和系统原生能力
* 除非确有必要，否则不引入第三方 UI 框架
* 优先使用 Swift Concurrency，不使用过时的回调式异步实现
* 页面导航优先使用 `NavigationStack`
* 状态管理优先使用 SwiftUI 原生 Observation 机制

### 2.2 类型系统

项目统一使用 Swift 类型系统。

要求：

* 新增代码默认使用 Swift。
* 禁止为了快速解决类型问题而大量使用 `Any`、强制转换或强制解包。
* 优先定义明确的业务模型、协议、枚举和错误类型。
* 对外部 API、原生模块返回值进行必要的类型约束。
* 并发边界上的值应满足必要的 `Sendable` 约束。
* 不允许通过关闭 Swift 编译检查掩盖真实问题。

---

## 3. Agent 工作原则

### 3.1 开始开发前

Agent 在修改代码之前必须：

1. 阅读 `AGENTS.md`。
2. 检查项目目录结构。
3. 检查已有开发文档。
4. 检查相关 View、Store、Model、Service 和工具类型是否已经存在。
5. 检查 `project.yml`、Xcode 工程及现有依赖。
6. 优先复用现有实现，而不是重新创建重复代码。

如果项目已有约定与 Agent 的默认习惯不同：

> **以项目现有约定为准。**

---

### 3.2 修改代码原则

Agent 必须遵循：

* 小步修改。
* 单一职责。
* 优先简单实现。
* 避免过度设计。
* 避免为了一个小功能引入大型依赖。
* 不随意修改无关代码。
* 不随意升级项目依赖。
* 不随意改变目录结构。
* 不删除看似无用但可能被其他模块使用的代码。
* 修改公共组件或公共工具前，需要确认其影响范围。

如果发现现有代码存在问题：

* 与当前任务直接相关的问题，可以一并修复。
* 与当前任务无关的问题，不应擅自大规模重构。
* 如果问题可能影响后续开发，应记录到开发文档或 TODO 中。

---

## 4. UI 与设备适配要求

所有 UI 必须遵循 MacOS 原生交互

### 必须关注

* 屏幕尺寸。
* 安全区域。
* 深色/浅色系统环境。
* MacOS 不同系统版本的原生 API 差异。
* 权限申请流程。
* 横竖屏行为。

禁止：

* 只针对单一设备尺寸写死布局。
* 依赖未公开的系统私有行为。
* 使用大量绝对定位实现普通页面布局。

---

## 5. 原生能力使用规范

涉及相册、文件、音频、相机、权限、通知等原生能力时：

1. 优先使用 Apple 官方 Framework。
2. 确认当前的支持情况。
3. 明确处理权限被拒绝的情况。
4. 明确处理用户取消操作的情况。
5. 明确处理设备不支持的情况。
6. 明确处理系统异常和资源不可用情况。
7. 不允许假设用户一定授予权限。

涉及原生能力的代码必须考虑：

```text
正常流程
↓
用户拒绝
↓
用户取消
↓
系统异常
↓
设备不支持
↓
资源释放
```

尤其是音频、视频、文件等资源，必须注意生命周期和资源释放，避免：

* 内存泄漏
* 文件句柄泄漏
* 音频对象未释放
* 重复初始化
* 重复监听
* 页面退出后仍然执行回调

---

## 6. 状态管理规范

状态应按照以下原则划分：

### 本地 UI 状态

例如：

* 弹窗是否显示
* 当前选中项
* UI 展开状态
* 临时输入状态

优先使用 SwiftUI `@State` 或 View 内的临时状态。

### 全局客户端状态

只有确实需要跨页面共享的状态才进入 `@Observable` Store，并通过 SwiftUI Environment 注入。

### 服务端状态

API 数据、缓存、请求状态和重试由对应 Store / Service 统一管理。

禁止：

* 所有状态无论大小都塞进全局 Store。
* 将服务端数据长期复制到多个本地 Store。
* 在多个页面重复维护同一份服务端状态。

---

## 7. 组件设计规范

组件应尽量遵循：

```text
SwiftUI 页面 View
 ├── 业务子 View
 ├── 通用 View
 ├── Model
 ├── Store
 └── Service / 系统 Framework 适配器
```

原则：

* 页面 View 负责组合。
* 子 View 负责可复用 UI。
* Model 负责稳定的数据表达和纯逻辑。
* Service 负责网络、文件和系统 Framework 交互。
* Store 负责共享状态与业务流程编排。

避免创建：

* 超大页面组件。
* 一个文件包含大量无关逻辑。
* 同一个业务逻辑复制到多个页面。

当组件出现明显复杂度增长时，应考虑拆分。

---

## 8. 错误处理

所有异步操作都必须考虑失败情况。

至少覆盖：

* 网络异常
* 请求超时
* 服务端错误
* 用户取消
* 权限不足
* 文件不存在
* 文件损坏
* 数据格式异常
* 原生模块异常

禁止：

```swift
try {
    try await doSomething()
} catch {
}
```

除非明确说明该异常可以安全忽略。

错误应：

1. 记录必要日志。
2. 给用户合理反馈。
3. 保证页面不会进入不可恢复状态。
4. 必要时提供重试能力。

禁止向用户直接展示底层异常堆栈、敏感信息或内部实现细节。

---

## 9. 测试要求

### 9.1 基本原则

**每完成一个功能模块，都必须进行测试。**

不能出现：

```text
写代码
↓
认为应该没问题
↓
继续开发
```

必须：

```text
实现
↓
检查
↓
测试
↓
修复
↓
再次测试
↓
更新文档
↓
继续下一个功能
```

### 9.2 测试层级

根据功能复杂程度选择：

* Swift 编译与类型检查
* Xcode Build
* Swift Testing / XCTest 单元测试
* SwiftUI UI 测试
* 集成测试
* 真机测试

涉及原生能力时，**真机测试优先于模拟器测试**。

### 9.3 每个功能完成后必须验证

至少确认：

* 正常流程可用。
* 空数据状态正常。
* 加载状态正常。
* 错误状态正常。
* 用户取消操作正常。
* 权限异常正常。
* 页面返回正常。
* 重复操作不会导致异常。
---

## 10. 文档维护机制

项目必须维护独立的开发文档。

建议目录：

```text
docs/
├── development.md
├── architecture.md
├── modules/
│   └── ...
└── decisions/
    └── ...
```

### 10.1 功能完成后必须更新文档

每完成一个功能模块，Agent 必须同步更新对应开发文档。

至少记录：

```text
功能名称
实现状态
实现方式
涉及文件
技术决策
关键实现细节
测试情况
已知问题
后续扩展方向
```

### 10.2 技术决策

当 Agent 做出重要技术选择时，应记录：

* 为什么选择该方案。
* 放弃了哪些方案。
* 当前方案的优缺点。
* 未来什么情况下需要重新评估。

避免项目后续开发人员无法理解历史决策。

### 10.3 文档真实性

文档必须反映当前真实代码状态。

禁止：

* 功能已经删除但文档仍然描述为已实现。
* 功能没有完成却标记为完成。
* 测试没有执行却写成测试通过。
* 虚构测试结果。
* 虚构 API。
* 虚构技术方案。

---

## 11. Git 与变更规范

Agent 不应执行危险 Git 操作。

默认允许：

* 查看状态
* 查看 diff
* 查看 log
* 创建普通 commit（用户明确要求时）
* 创建开发分支（用户明确要求时）

执行以下操作前必须获得用户明确授权：

* `git reset --hard`
* `git clean`
* 强制 push
* 删除远程分支
* 大规模删除文件
* 覆盖用户未提交的修改
* 修改历史提交
* `rebase` 可能造成提交丢失的操作

原则：

> **任何可能导致用户代码、提交或数据不可恢复的操作，都必须先获得明确授权。**

---

## 12. 数据与安全边界

Agent 不得：

* 泄露 API Key。
* 泄露 Token。
* 泄露密码。
* 将 `.env` 内容提交到 Git。
* 将私密用户数据写入日志。
* 将访问令牌硬编码到客户端。
* 在代码中保存真实生产环境凭证。
* 擅自上传用户数据到第三方服务。

敏感配置应通过环境变量或安全配置管理。

日志中不得输出：

* 密码
* Token
* Authorization Header
* 私有文件内容
* 用户隐私数据
* 未脱敏的敏感标识

---

## 13. 文件与用户数据安全

涉及用户文件时：

* 不得擅自读取与当前任务无关的用户文件。
* 不得擅自复制用户文件到第三方服务。
* 不得为了调试长期保存用户原始数据。
* 测试数据优先使用模拟数据。
* 需要真实数据测试时，应明确告知用户。

任何涉及用户数据上传、删除、覆盖或批量处理的操作，都必须确认操作范围。

---

## 14. 依赖管理

新增依赖前必须判断：

1. Apple 官方 Framework 是否已经提供对应能力。
2. 项目现有代码或依赖是否已经能够解决问题。
3. 新依赖是否支持项目的最低 MacOS 版本。
4. 新依赖是否与当前 Swift / Xcode 工具链兼容。
5. 是否必须引入新的 Swift Package 或二进制 Framework。
6. 是否增加原生构建复杂度、包体积或启动成本。
7. 是否增加维护成本。

**能用现有依赖解决的问题，不新增依赖。**

禁止为了方便随意安装多个功能相近的库。

---

## 15. 性能要求

移动端尤其关注：

* 图片加载性能。
* 列表渲染性能。
* 内存占用。
* 页面切换性能。
* 网络请求数量。
* 大文件处理。
* 音频资源生命周期。

对于大量数据：

* 使用虚拟化列表。
* 避免一次性加载大量资源。
* 避免不必要的重复渲染。
* 避免在主线程执行高成本操作。
* 对媒体资源进行合理缓存和处理。

---

## 16. Agent 的禁止行为

Agent 默认禁止：

* 为了“看起来更完整”而添加用户未要求的功能。
* 擅自改变产品需求。
* 擅自改变技术架构。
* 擅自更换技术栈。
* 大规模重构无关代码。
* 删除已有功能。
* 修改生产配置。
* 操作真实生产数据。
* 上传用户隐私数据。
* 提交敏感信息。
* 绕过测试。
* 虚构测试结果。
* 虚构文档内容。
* 使用 TODO 掩盖尚未解决的关键问题。

---

## 17. 高风险操作确认机制

以下操作必须先向用户说明影响并获得明确确认：

### 数据风险

* 删除数据库数据。
* 批量删除文件。
* 覆盖用户数据。
* 修改生产数据。
* 执行不可逆迁移。

### 代码风险

* 大规模重构。
* 删除公共组件。
* 删除核心业务代码。
* 修改基础架构。
* 升级核心框架。
* 大规模升级依赖。

### 环境风险

* 修改生产环境。
* 修改 CI/CD。
* 修改服务器配置。
* 修改域名、证书、DNS。
* 修改权限策略。

### Git 风险

* 强制 push。
* reset 未提交代码。
* 删除分支。
* 修改历史提交。
* 清理未跟踪文件。

---

## 18. 完成功能的标准

一个功能只有同时满足以下条件，才能认为开发完成：

* [ ] 代码实现完成
* [ ] Swift 编译与类型检查通过
* [ ] Xcode Build 通过
* [ ] 正常流程测试通过
* [ ] 异常流程测试通过
* [ ] 必要的 MacOS 测试通过
* [ ] 必要的真机测试通过
* [ ] 无明显资源泄漏
* [ ] 无明显性能问题
* [ ] 相关开发文档已更新
* [ ] 技术决策已记录（如有）
* [ ] 已知问题已记录

**代码完成 ≠ 功能完成。**

只有代码、测试和文档均完成后，Agent 才应将该功能标记为完成。
